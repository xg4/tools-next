import { PropsWithChildren } from 'react'

export const metadata = {
  title: '解析 Request Headers',
  description: '工具集合',
}

export default function Layout(props: PropsWithChildren) {
  return <>{props.children}</>
}
